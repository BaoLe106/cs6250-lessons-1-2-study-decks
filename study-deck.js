(()=>{const screens=[...document.querySelectorAll('.screen')],names=[...document.querySelectorAll('.screen')].map(x=>x.dataset.title||'Topic');let i=0;const prev=document.querySelector('[data-prev]'),next=document.querySelector('[data-next]'),count=document.querySelector('[data-count]'),bar=document.querySelector('.progress i');function show(n){i=Math.max(0,Math.min(screens.length-1,n));screens.forEach((s,x)=>{s.classList.toggle('active',x===i);s.classList.toggle('before',x<i)});prev.disabled=i===0;next.disabled=i===screens.length-1;count.innerHTML=`<b>${i+1}</b> / ${screens.length} · ${names[i]}`;bar.style.width=`${100*(i+1)/screens.length}%`;history.replaceState(null,'',`#${i+1}`)}prev.onclick=()=>show(i-1);next.onclick=()=>show(i+1);addEventListener('keydown',e=>{if(['ArrowRight','PageDown',' '].includes(e.key)){e.preventDefault();show(i+1)}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();show(i-1)}});let x;addEventListener('touchstart',e=>x=e.changedTouches[0].clientX,{passive:true});addEventListener('touchend',e=>{if(x==null)return;let d=e.changedTouches[0].clientX-x;if(Math.abs(d)>55)show(i+(d<0?1:-1));x=null},{passive:true});document.querySelectorAll('.q button').forEach(b=>b.onclick=()=>b.parentElement.classList.toggle('open'));const h=+location.hash.slice(1);show(h>0?h-1:0)})();
